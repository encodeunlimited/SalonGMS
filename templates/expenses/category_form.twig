{% extends "layout.twig" %}

{% block content %}
<div class="mb-8">
    <h2 class="text-2xl font-bold text-gray-900 tracking-tight">{{ title }}</h2>
    <p class="text-sm text-gray-500 mt-1">
        {% if category %}Update the details for this expense category.{% else %}Add a new category to organize your operational costs.{% endif %}
    </p>
</div>

<div class="bg-white rounded-xl shadow-sm border border-gray-100 max-w-lg">
    <form action="{% if category %}/web/expenses/categories/{{ category.id }}/edit{% else %}/web/expenses/categories{% endif %}" method="POST" class="p-6 space-y-6">
        
        <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Category Name <span class="text-red-500">*</span></label>
            <input type="text" name="name" value="{{ category.name|default('') }}" required placeholder="e.g. Office Supplies" class="w-full border-gray-300 rounded-lg shadow-sm focus:ring-salon-accent focus:border-salon-accent sm:text-sm">
        </div>
        
        <div class="flex items-center space-x-3 pt-4 border-t border-gray-100">
            <button type="submit" class="bg-salon-dark text-white px-5 py-2.5 rounded-lg text-sm font-medium hover:bg-salon-navy shadow-sm transition-colors">
                {% if category %}Save Changes{% else %}Create Category{% endif %}
            </button>
            <a href="/web/expenses/categories" class="bg-gray-100 text-gray-700 px-5 py-2.5 rounded-lg text-sm font-medium hover:bg-gray-200 transition-colors">
                Cancel
            </a>
        </div>
    </form>
</div>
{% endblock %}
