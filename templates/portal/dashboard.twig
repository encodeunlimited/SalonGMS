{% extends 'portal/layout.twig' %}

{% block content %}
<div class="animate-fade-in">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 2rem;">
        <div>
            <h1 style="font-size: 2.5rem; font-weight: 700; margin-bottom: 0.5rem;">Welcome, {{ customer.name }}</h1>
            <div style="display: inline-flex; flex-wrap: wrap; gap: 0.5rem; align-items: center;">
                <div style="display: inline-flex; align-items: center; background-color: var(--primary-container); color: var(--on-primary-container); padding: 0.5rem 1rem; border-radius: 9999px; font-weight: 600;">
                    <span class="material-symbols-outlined" style="margin-right: 0.5rem; font-size: 1.25rem;">stars</span>
                    {{ customer.loyalty_points|default(0) }} Loyalty Points
                </div>
                {% if customer.credit_balance|default(0) > 0 %}
                <div style="display: inline-flex; align-items: center; background-color: #fee2e2; color: #991b1b; padding: 0.5rem 1rem; border-radius: 9999px; font-weight: 600; border: 1px solid #fca5a5;">
                    <svg style="width: 1.25rem; height: 1.25rem; margin-right: 0.5rem;" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                    QAR {{ customer.credit_balance|number_format(2) }} Due
                </div>
                {% endif %}
            </div>
            <div style="margin-top: 1rem;">
                <a href="/portal/profile" style="color: var(--primary); font-weight: 500; font-size: 0.875rem; display: inline-flex; align-items: center; text-decoration: none;" onmouseover="this.style.textDecoration='underline'" onmouseout="this.style.textDecoration='none'">
                    <svg style="width: 1rem; height: 1rem; margin-right: 0.25rem;" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z"></path></svg>
                    Edit Profile
                </a>
            </div>
        </div>
        <div style="display: flex; gap: 1rem; align-items: center;">
            <button x-data @click="$dispatch('open-rating-modal')" class="btn-primary" style="background-color: var(--salon-accent, #ca8a04); color: #fff; width: auto; border: none; cursor: pointer; display: flex; align-items: center; gap: 0.5rem;">
                <svg style="width: 1.25rem; height: 1.25rem;" fill="currentColor" viewBox="0 0 20 20"><path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"></path></svg>
                Rate Our Salon
            </button>
            <a href="/portal/book" class="btn-primary" style="width: auto;">Book New Appointment</a>
        </div>
    </div>

    {% if is_birthday %}
    <div style="background: linear-gradient(135deg, #fce7f3 0%, #fbcfe8 100%); border: 1px solid #f9a8d4; border-radius: 1rem; padding: 2rem; margin-bottom: 2rem; display: flex; justify-content: space-between; align-items: center; box-shadow: 0 4px 6px rgba(0,0,0,0.05);">
        <div>
            <h2 style="font-size: 1.75rem; font-weight: 800; color: #831843; margin-bottom: 0.5rem;">🎉 Happy Birthday, {{ customer.name }}! 🎂</h2>
            <p style="color: #9d174d; font-size: 1rem; margin-bottom: 0;">We hope you have a fantastic day! Treat yourself to one of our special beauty packages.</p>
        </div>
        <a href="/portal/book" class="btn-primary" style="background-color: #db2777; width: auto; font-weight: bold; padding: 0.75rem 1.5rem;">View Birthday Offers</a>
    </div>
    {% endif %}

    <div x-data="{ activeTab: 'upcoming' }" style="margin-bottom: 2rem;">
        <div style="display: flex; gap: 2rem; border-bottom: 1px solid var(--border-color); margin-bottom: 1.5rem; padding-left: 0.25rem;">
            <a href="#" @click.prevent="activeTab = 'upcoming'" 
                 :style="activeTab === 'upcoming' ? 'border-bottom-color: var(--primary); color: var(--primary); font-weight: 600;' : 'border-bottom-color: transparent; color: var(--text-muted); font-weight: 500;'"
                 style="text-decoration: none; border-bottom: 2px solid transparent; padding: 0 0 0.85rem 0; font-size: 0.95rem; cursor: pointer !important; transition: all 0.2s; margin-bottom: -1px;"
                 onmouseover="if(activeTab !== 'upcoming') this.style.color='var(--text-main)'"
                 onmouseout="if(activeTab !== 'upcoming') this.style.color='var(--text-muted)'">
                Upcoming Appointments
            </a>
            <a href="#" @click.prevent="activeTab = 'past'" 
                 :style="activeTab === 'past' ? 'border-bottom-color: var(--primary); color: var(--primary); font-weight: 600;' : 'border-bottom-color: transparent; color: var(--text-muted); font-weight: 500;'"
                 style="text-decoration: none; border-bottom: 2px solid transparent; padding: 0 0 0.85rem 0; font-size: 0.95rem; cursor: pointer !important; transition: all 0.2s; margin-bottom: -1px;"
                 onmouseover="if(activeTab !== 'past') this.style.color='var(--text-main)'"
                 onmouseout="if(activeTab !== 'past') this.style.color='var(--text-muted)'">
                Past Appointments
            </a>
        </div>

        <!-- Upcoming Appointments -->
        <div x-show="activeTab === 'upcoming'">
            {% if upcoming_appointments|length > 0 %}
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                {% for app in upcoming_appointments %}
                    <div class="card glass mb-4" style="padding: 1.5rem;">
                        <div style="display: flex; justify-content: space-between; align-items: flex-start; gap: 1rem; margin-bottom: 0.5rem;">
                            <div style="display: flex; flex-direction: column;">
                                <span style="font-weight: 700; font-size: 1.2rem; color: var(--text-main);">
                                    {{ app.service }}
                                </span>
                                <span style="font-size: 0.9rem; font-weight: 600; color: var(--salon-accent, #ca8a04); margin-top: 0.15rem;">
                                    {% if '(Package Redemption)' in app.service %}
                                        QAR 0.00
                                    {% else %}
                                        QAR {{ app.invoice_amount|default(app.service_price|default(0))|number_format(2) }}
                                    {% endif %}
                                </span>
                            </div>
                            {% if app.status == 'scheduled' or app.status == 'pending' %}
                                <span style="background-color: #dbeafe; color: #1e40af; padding: 0.25rem 0.75rem; border-radius: 9999px; font-size: 0.75rem; font-weight: 600; text-transform: uppercase; white-space: nowrap;">{{ app.status }}</span>
                            {% elseif app.status == 'approved' %}
                                <span style="background-color: #dbeafe; color: #2563eb; padding: 0.25rem 0.75rem; border-radius: 9999px; font-size: 0.75rem; font-weight: 600; text-transform: uppercase; white-space: nowrap;">{{ app.status }}</span>
                            {% elseif app.status == 'done' %}
                                <span style="background-color: #d1fae5; color: #10b981; padding: 0.25rem 0.75rem; border-radius: 9999px; font-size: 0.75rem; font-weight: 600; text-transform: uppercase; white-space: nowrap;">{{ app.status }}</span>
                            {% elseif app.status == 'paid' or app.status == 'completed' %}
                                <span style="background-color: #d1fae5; color: #065f46; padding: 0.25rem 0.75rem; border-radius: 9999px; font-size: 0.75rem; font-weight: 600; text-transform: uppercase; white-space: nowrap;">Paid</span>
                            {% elseif app.status == 'cancelled' %}
                                <span style="background-color: #fee2e2; color: #991b1b; padding: 0.25rem 0.75rem; border-radius: 9999px; font-size: 0.75rem; font-weight: 600; text-transform: uppercase; white-space: nowrap;">Cancelled</span>
                            {% else %}
                                <span style="background-color: #e5e7eb; color: #374151; padding: 0.25rem 0.75rem; border-radius: 9999px; font-size: 0.75rem; font-weight: 600; text-transform: uppercase; white-space: nowrap;">{{ app.status }}</span>
                            {% endif %}
                        </div>
                        <div style="margin-bottom: 0.75rem; color: var(--text-muted); font-size: 0.95rem;">
                            With <span style="font-weight: 500; color: var(--text-main);">{{ app.stylist }}</span>
                        </div>
                        <div class="text-muted" style="display: flex; align-items: center; gap: 0.5rem; font-size: 0.9rem;">
                            🕒 {{ app.start_time|date("l, M j, Y") }} | {{ app.start_time|date("g:i A") }} - {{ app.end_time|date("g:i A") }}
                        </div>
                    </div>
                {% endfor %}
                </div>
            {% else %}
                <div class="card glass text-center text-muted" style="padding: 2rem;">
                    No upcoming appointments found.
                </div>
            {% endif %}
        </div>

        <!-- Past Appointments -->
        <div x-show="activeTab === 'past'" style="display: none;">
            {% if past_appointments|length > 0 %}
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                {% for app in past_appointments %}
                    <div class="card glass mb-4" style="padding: 1.5rem; opacity: 0.8;">
                        <div style="display: flex; justify-content: space-between; align-items: flex-start; gap: 1rem; margin-bottom: 0.25rem;">
                            <div style="display: flex; flex-direction: column;">
                                <span style="font-weight: 600; color: var(--text-main);">
                                    {{ app.service }}
                                </span>
                                <span style="font-size: 0.85rem; font-weight: 600; color: var(--salon-accent, #ca8a04); margin-top: 0.15rem;">
                                    {% if '(Package Redemption)' in app.service %}
                                        QAR 0.00
                                    {% else %}
                                        QAR {{ app.invoice_amount|default(app.service_price|default(0))|number_format(2) }}
                                    {% endif %}
                                </span>
                            </div>
                            {% if app.status == 'cancelled' %}
                                <span style="color: #991b1b; font-size: 0.75rem; font-weight: 700; text-transform: uppercase; white-space: nowrap;">Cancelled</span>
                            {% elseif app.invoice_status == 'paid' %}
                                <span style="color: #065f46; font-size: 0.75rem; font-weight: 700; text-transform: uppercase; white-space: nowrap;">
                                    Paid {% if app.payment_method %}({{ app.payment_method }}){% endif %}
                                </span>
                            {% elseif app.invoice_status == 'unpaid' %}
                                <span style="color: #9a3412; font-size: 0.75rem; font-weight: 700; text-transform: uppercase; white-space: nowrap;">Unpaid</span>
                            {% elseif '(Package Redemption)' in app.service %}
                                <span style="color: #065f46; font-size: 0.75rem; font-weight: 700; text-transform: uppercase; white-space: nowrap;">Redeemed</span>
                            {% elseif app.status == 'done' %}
                                <span style="color: #10b981; font-size: 0.75rem; font-weight: 700; text-transform: uppercase; white-space: nowrap;">Done</span>
                            {% else %}
                                <span class="text-muted" style="font-size: 0.75rem; font-weight: 700; text-transform: uppercase; white-space: nowrap;">{{ app.status }}</span>
                            {% endif %}
                        </div>
                        <div class="text-muted" style="font-size: 0.85rem; margin-bottom: 0.5rem;">
                            With {{ app.stylist }}
                        </div>
                        <div class="text-muted" style="font-size: 0.85rem;">
                            {{ app.start_time|date("M j, Y") }} at {{ app.start_time|date("g:i A") }}
                        </div>
                    </div>
                {% endfor %}
                </div>
            {% else %}
                <div class="card glass text-center text-muted" style="padding: 2rem;">
                    No past appointments.
                </div>
            {% endif %}
        </div>
    </div>
    
    {% if my_packages|length > 0 %}
    <div style="margin-top: 2rem;">
        <h2 style="font-size: 1.5rem; margin-bottom: 1rem;">My Packages</h2>
        <div class="grid grid-cols-2 gap-4">
            {% for package in my_packages %}
                <div class="card glass" style="padding: 1.5rem;">
                    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1rem;">
                        <h3 style="font-size: 1.25rem; font-weight: 700; margin: 0; color: var(--primary);">{{ package.package_name }}</h3>
                        {% if package.expires_at %}
                            <span style="font-size: 0.75rem; color: var(--text-muted); background: var(--bg-elevated); padding: 0.25rem 0.5rem; border-radius: 9999px;">
                                Expires: {{ package.expires_at|date("M j, Y") }}
                            </span>
                        {% endif %}
                    </div>
                    
                    <div style="display: flex; flex-direction: column; gap: 0.75rem;">
                        {% for service in package.services %}
                            <div style="display: flex; justify-content: space-between; align-items: center; border-bottom: 1px dashed var(--border-color); padding-bottom: 0.5rem;">
                                <div style="display: flex; flex-direction: column;">
                                    <span style="font-weight: 500; font-size: 0.95rem; color: var(--text-main);">{{ service.service_name }}</span>
                                    <span style="font-size: 0.8rem; color: var(--text-muted);">{{ service.remaining }} / {{ service.total }} left</span>
                                </div>
                                <a href="/portal/book?service_id=cps_{{ service.cps_id }}" class="btn-primary" style="padding: 0.25rem 0.75rem; font-size: 0.75rem; width: auto; border-radius: 9999px; text-decoration: none;">Book</a>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
    {% endif %}
</div>

<!-- Rating Modal -->
<div x-data="{ show: false, rating: 0, hoverRating: 0, comment: '', submitting: false }"
     x-on:open-rating-modal.window="show = true"
     x-show="show" 
     style="display: none; position: fixed; inset: 0; z-index: 50; overflow-y: auto;" 
     aria-labelledby="modal-title" role="dialog" aria-modal="true">
    <div style="display: flex; align-items: flex-end; justify-content: center; min-height: 100vh; padding: 1rem; text-align: center; sm:block sm:p-0;">
        <div x-show="show" x-transition.opacity class="fixed inset-0 bg-gray-500 bg-opacity-75 transition-opacity" aria-hidden="true" @click="show = false"></div>
        <span class="hidden sm:inline-block sm:align-middle sm:h-screen" aria-hidden="true">&#8203;</span>
        <div x-show="show" x-transition
             style="display: inline-block; align-items: bottom; background-color: white; border-radius: 0.5rem; text-align: left; overflow: hidden; box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04); transform: transition-all; sm:my-8; sm:align-middle; sm:max-w-lg; sm:w-full;">
            <div style="background-color: white; padding: 1.5rem;">
                <div style="display: flex; flex-direction: column; align-items: center;">
                    <div style="margin-left: auto; cursor: pointer; color: #9ca3af;" @click="show = false">
                        <svg style="width: 1.5rem; height: 1.5rem;" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>
                    </div>
                    <div style="text-align: center; margin-top: 0.5rem; margin-bottom: 1.5rem;">
                        <h3 style="font-size: 1.25rem; font-weight: 600; color: #111827;" id="modal-title">Rate Our Salon</h3>
                        <p style="font-size: 0.875rem; color: #6b7280; margin-top: 0.5rem;">We value your feedback. Let us know how your experience was!</p>
                    </div>

                    <div style="display: flex; gap: 0.5rem; margin-bottom: 1.5rem; justify-content: center;">
                        <template x-for="i in 5">
                            <svg @click="rating = i" 
                                 @mouseenter="hoverRating = i"
                                 @mouseleave="hoverRating = 0"
                                 :style="{'color': (hoverRating >= i || rating >= i) ? 'var(--salon-accent, #ca8a04)' : '#d1d5db', 'cursor': 'pointer', 'width': '2.5rem', 'height': '2.5rem', 'transition': 'color 0.2s'}" 
                                 fill="currentColor" viewBox="0 0 20 20">
                                <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"></path>
                            </svg>
                        </template>
                    </div>

                    <div style="width: 100%; margin-bottom: 1rem;">
                        <label for="comment" style="display: block; font-size: 0.875rem; font-weight: 500; color: #374151; margin-bottom: 0.25rem;">Comment (Optional)</label>
                        <textarea x-model="comment" id="comment" rows="3" style="width: 100%; padding: 0.5rem; border: 1px solid #d1d5db; border-radius: 0.375rem; box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05); focus:outline-none; focus:border-var(--salon-accent);"></textarea>
                    </div>
                </div>
            </div>
            <div style="background-color: #f9fafb; padding: 1rem 1.5rem; display: flex; flex-direction: row-reverse;">
                <button @click="
                    submitting = true;
                    fetch('/portal/rating', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ rating: rating, comment: comment })
                    })
                    .then(r => r.json())
                    .then(data => {
                        submitting = false;
                        if(data.success) {
                            show = false;
                            rating = 0;
                            comment = '';
                            alert('Thank you for your rating!');
                        } else {
                            alert(data.error || 'Something went wrong');
                        }
                    })
                    .catch(e => {
                        submitting = false;
                        alert('Error submitting rating');
                    });
                " :disabled="rating === 0 || submitting" type="button" class="btn-primary" style="width: auto; margin-left: 0.75rem;" :style="{'opacity': (rating === 0 || submitting) ? '0.5' : '1'}">
                    <span x-show="!submitting">Submit</span>
                    <span x-show="submitting">Submitting...</span>
                </button>
                <button @click="show = false" type="button" style="margin-top: 0; width: auto; background-color: white; border: 1px solid #d1d5db; border-radius: 0.375rem; padding: 0.5rem 1rem; color: #374151; font-weight: 500; cursor: pointer;">Cancel</button>
            </div>
        </div>
    </div>
</div>

{% endblock %}
